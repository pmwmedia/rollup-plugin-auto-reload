{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js src/**/*"
  },
  "devDependencies": {
    "eslint": "8.57.1",
    "rollup": "4.34.2",
    "rollup-plugin-auto-reload": ".."
  }
}

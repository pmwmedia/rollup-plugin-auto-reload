{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js src/**/*"
  },
  "devDependencies": {
    "eslint": "9.24.0",
    "rollup": "4.39.0",
    "rollup-plugin-auto-reload": ".."
  }
}

{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js src/**/*"
  },
  "devDependencies": {
    "eslint": "8.35.0",
    "rollup": "3.19.0",
    "rollup-plugin-auto-reload": ".."
  }
}

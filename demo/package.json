{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js src/**/*"
  },
  "devDependencies": {
    "eslint": "8.41.0",
    "rollup": "3.21.6",
    "rollup-plugin-auto-reload": ".."
  }
}

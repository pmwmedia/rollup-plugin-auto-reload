{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js src/**/*"
  },
  "devDependencies": {
    "eslint": "8.39.0",
    "rollup": "3.20.7",
    "rollup-plugin-auto-reload": ".."
  }
}

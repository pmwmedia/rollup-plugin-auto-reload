{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js *.mjs src/**/*",
    "fix": "eslint *.js *.mjs src/**/* --fix"
  },
  "devDependencies": {
    "eslint": "9.33.0",
    "rollup": "4.47.1",
    "rollup-plugin-auto-reload": ".."
  }
}

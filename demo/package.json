{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js src/**/*"
  },
  "devDependencies": {
    "eslint": "8.56.0",
    "rollup": "4.9.5",
    "rollup-plugin-auto-reload": ".."
  }
}

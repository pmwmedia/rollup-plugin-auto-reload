{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js src/**/*"
  },
  "devDependencies": {
    "eslint": "8.56.0",
    "rollup": "4.12.0",
    "rollup-plugin-auto-reload": ".."
  }
}

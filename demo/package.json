{
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch",
    "lint": "eslint *.js src/**/*"
  },
  "devDependencies": {
    "eslint": "8.57.1",
    "rollup": "4.24.1",
    "rollup-plugin-auto-reload": ".."
  }
}
